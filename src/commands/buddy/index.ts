// Auto-generated stub — no command registered yet.
// Returning null instead of {} so commands.ts:324 (`...(buddy ? [buddy] : [])`)
// evaluates falsy and skips this entry. An empty object would pass the truthy
// check and pollute the command registry with a nameless entry, breaking
// sort-by-name in HelpV2 and the slash typeahead picker.
import type { Command } from '../../commands.js'
const buddyCommand: Command | null = null
export default buddyCommand
